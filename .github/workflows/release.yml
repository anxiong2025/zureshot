name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS Universal (Apple Silicon + Intel in one binary)
          - platform: macos-latest
            args: '--target universal-apple-darwin'
            target: universal-apple-darwin

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: v__VERSION__
          releaseName: 'Zureshot v__VERSION__'
          releaseBody: |
            ## 🎉 Zureshot v__VERSION__

            ### 下载安装
            下载 `.dmg` 文件，打开后拖入 Applications 即可。
            **适用于所有 Mac（Apple Silicon M1/M2/M3/M4 及 Intel）**

            ### 更新说明
            如果你已安装 Zureshot，应用会自动检测并提示更新。

            ---
            > macOS 可能提示"无法验证开发者"，请在「系统设置 → 隐私与安全性」中点击「仍要打开」。
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
