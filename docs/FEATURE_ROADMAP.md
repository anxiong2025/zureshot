# Zureshot 功能路线图

> 当前版本：**v0.5.1-beta**（跨平台：macOS + Linux）
> 创建日期：2026-02-19
> 目标：从截屏/录屏工具进化为 **开源版 CleanShot X + Screen Studio**

---

## 版本规划总览

```
v0.5.1-beta  当前版本 ✅  Linux 首发 + 跨平台基础
v0.6.0       摄像头气泡 + 截图钉
v0.7.0       虚拟背景 + OCR 文字提取
v0.8.0       实时标注 + 演示增强
v0.9.0       粗剪导出 + LUT 滤镜 + 云端分享
v1.0.0       稳定版发布
```

---

## 🔲 v0.5.x — 稳定性修复

> 目标：确保 v0.5.1-beta 在真实 Linux 机器上稳定运行
> 前置条件：拿到 Linux 实机 / 社区反馈

- [ ] **0.5.1** Linux 实机验证：.deb 安装 → 启动 → 截屏 → 录屏全流程
- [ ] **0.5.2** Linux 实机测试矩阵：Ubuntu 24.04 GNOME Wayland + X11
- [ ] **0.5.3** 修复实机测试发现的 Bug
- [ ] **0.5.4** HiDPI 缩放测试（100%/125%/150%/200%）
- [ ] **0.5.5** 自动更新端到端验证（v0.5.x → v0.5.x+1 升级测试）
- [ ] **0.5.6** 性能基准：录制 CPU/内存/帧率 vs macOS 版对比
- [ ] **0.5.7** 标记 Linux 为 stable（移除 beta 标记），发布 v0.5.x 正式版

---

## 🎥 v0.6.0 — 摄像头气泡 + 截图钉

> 预估工时：**2-3 天**
> 核心卖点：摄像头圆形浮窗——这是 Loom/Screen Studio 的标志性功能
> 跨平台：WebView + getUserMedia，macOS 和 Linux 共用同一套前端代码

### Phase A：摄像头气泡（Camera Bubble）

**技术方案：**
```
camera-overlay.html (独立 WebView 窗口)
  └─ CameraOverlay.svelte
       └─ navigator.mediaDevices.getUserMedia({ video: true })
       └─ <video> 元素 + CSS border-radius: 50% 圆形裁剪
       └─ 窗口属性：always-on-top, 透明背景, 可拖拽
  
录屏时：摄像头窗口是独立 OS 窗口 → SCK/PipeWire 自动捕获
  无需合成！系统级别的 "免费" 画中画
```

- [ ] **A.1** 创建 `camera-overlay.html` + `src/camera-overlay.js` 入口文件
- [ ] **A.2** 创建 `src/lib/CameraOverlay.svelte` 组件
  - `getUserMedia({ video: { width: 320, height: 320 } })`
  - `<video autoplay muted>` 元素
  - CSS `border-radius: 50%` 圆形 + `object-fit: cover`
  - `backdrop-filter` 装饰边框（发光/阴影）
- [ ] **A.3** Rust 端 `commands.rs` 添加摄像头窗口管理
  - `open_camera_overlay()` — 创建透明、无边框、always-on-top 窗口
  - `close_camera_overlay()` — 关闭窗口
  - `toggle_camera_overlay()` — 切换显示
  - 窗口大小三档：Small (120px) / Medium (200px) / Large (320px)
- [ ] **A.4** `tauri.conf.json` 添加 `camera-overlay` 窗口配置
  - `transparent: true`, `decorations: false`, `alwaysOnTop: true`
  - `resizable: false`, `width/height` 默认 200
- [ ] **A.5** RecordingBar 添加摄像头切换按钮 📷
  - 点击切换摄像头窗口显示/隐藏
  - 长按或右键 → 选择摄像头设备 + 大小
- [ ] **A.6** 托盘菜单添加摄像头选项
  - 📷 Camera Bubble → On/Off
  - Size → Small / Medium / Large
- [ ] **A.7** 拖拽定位：用户可拖动气泡到任意屏幕位置
  - 记住上次位置（`settings.json` 持久化）
  - 磁吸效果：靠近屏幕边缘自动吸附
- [ ] **A.8** 摄像头形状切换：圆形 ↔ 圆角矩形 ↔ 方形

### Phase B：截图钉在桌面（Pin to Desktop）

> 对标 CleanShot X 的 "Pin" 功能——截图后钉在桌面随时参考

- [ ] **B.1** 创建 `pinned-screenshot.html` + `PinnedScreenshot.svelte`
  - 显示截图图片
  - 无边框窗口、always-on-top
  - 拖拽移动
  - 鼠标滚轮缩放
  - 右键菜单：复制 / 保存 / 关闭 / 透明度调节
- [ ] **B.2** Rust 端命令：`pin_screenshot(path)` / `unpin_screenshot(id)`
  - 支持同时钉多张截图
  - 每个钉窗口独立管理
- [ ] **B.3** ScreenshotPreview 添加 "📌 Pin" 按钮
  - 截图预览窗口 → 点击 📌 → 关闭预览 → 打开 Pin 窗口
- [ ] **B.4** 快捷键：`CmdOrCtrl+Shift+P` 钉住最近一张截图

---

## 🖼️ v0.7.0 — 虚拟背景 + OCR 文字提取

> 预估工时：**5-8 天**
> 核心卖点：录屏时自动模糊/替换摄像头背景 + 截图后一键识别文字

### Phase D：摄像头虚拟背景（Virtual Background）

**技术路线（渐进式）：**

| 阶段 | 方案 | 效果 | 性能 |
|------|------|------|------|
| Tier 1 | CSS `backdrop-filter: blur()` | 边缘粗糙 | GPU 0% |
| Tier 2 | MediaPipe Selfie Segmentation (WASM) | 实时抠图 | WebView 内 GPU |
| Tier 3 | 自定义虚拟背景图片 | 完全替换背景 | 同 Tier 2 |

- [ ] **D.1** Tier 1 — CSS 模糊背景
  - `CameraOverlay.svelte` 添加背景模糊效果
  - 简单的 CSS `filter: blur(10px)` 处理视频帧边缘
  - 适合背景颜色单一的场景
- [ ] **D.2** Tier 2 — MediaPipe AI 分割
  - 引入 `@mediapipe/selfie_segmentation` WASM 模块
  - Canvas 实时渲染：前景（人物）+ 模糊背景
  - 30fps 实时处理，WebView 内 GPU 加速
- [ ] **D.3** Tier 3 — 自定义虚拟背景
  - 用户上传背景图片
  - MediaPipe 分割 + Canvas 合成
  - 预设背景库（渐变、纯色、办公室等）
- [ ] **D.4** 背景设置 UI
  - 摄像头气泡右键菜单 → Background → None / Blur / Custom
  - 设置面板中预览效果

### Phase E：OCR 文字提取（截图识别文字）

> 对标 CleanShot X 的 OCR 功能——截图后自动识别文字并复制

**技术方案：**
```
macOS: Vision.framework (系统自带，零依赖)
  └─ VNRecognizeTextRequest → 离线识别，支持中英日韩

Linux: Tesseract.js (WASM，纯前端)
  └─ 或 tesseract-ocr 命令行调用
```

- [ ] **E.1** macOS OCR：Rust FFI 调用 Vision.framework
  - `VNRecognizeTextRequest` + `VNImageRequestHandler`
  - 支持中文、英文、日文、韩文
  - 离线运行，无网络依赖
- [ ] **E.2** Linux OCR：Tesseract.js 或 tesseract-ocr 命令行
  - WASM 方案：前端 WebView 内运行，无需系统安装
  - 命令行方案：`tesseract input.png stdout -l chi_sim+eng`
- [ ] **E.3** ScreenshotPreview 添加 "🔤 OCR" 按钮
  - 点击 → 识别 → 文本结果弹窗
  - 一键复制识别结果到剪贴板
  - 识别区域高亮显示
- [ ] **E.4** 选区 OCR：拖选截图区域 → 直接识别文字（不保存图片）
  - 新增快捷键 `CmdOrCtrl+Shift+O`（OCR 模式）
  - 选区 → 识别 → 自动复制 → 通知提示

---

## ✏️ v0.8.0 — 实时标注 + 演示增强

> 预估工时：**7-10 天**
> 核心卖点：录屏时实时画箭头/标注 + 自动缩放跟随鼠标

### Phase F：实时标注（Live Annotation）

> 录屏过程中实时在屏幕上绘制标注

- [ ] **F.1** 创建标注 overlay 层
  - 透明全屏窗口，z-index 最高
  - Canvas 2D 绘制引擎
  - 工具栏：✏️ 画笔 / ➡️ 箭头 / ⬜ 矩形 / ⭕ 椭圆 / 🔤 文字
- [ ] **F.2** 画笔工具
  - 自由绘制，压感支持（触控板）
  - 颜色选择器（红/黄/绿/蓝/白/黑）
  - 粗细调节（1-10px）
- [ ] **F.3** 箭头和形状工具
  - 拖拽绘制箭头、矩形、椭圆
  - 填充 / 描边切换
  - 编辑后可撤销 (Ctrl+Z)
- [ ] **F.4** 文字标注
  - 点击屏幕任意位置输入文字
  - 字号、颜色可调
  - 自动背景色增强可读性
- [ ] **F.5** 标注数据导出
  - 标注层 Canvas 叠加在录制画面上
  - 标注窗口被 SCK/PipeWire 自动捕获（同摄像头气泡方案）
  - 无需后期合成
- [ ] **F.6** 录制控制条增加标注开关 ✏️

### Phase G：演示增强（Presentation Enhancement）

> 对标 Screen Studio 的缩放跟随效果

- [ ] **G.1** 点击涟漪（Click Ripple）
  - 鼠标点击时显示扩散波纹动画
  - 左键/右键/双击 不同颜色
  - 透明 overlay 窗口渲染
- [ ] **G.2** 按键显示（Keystroke Overlay）
  - 实时显示键盘快捷键组合
  - 屏幕底部/角落半透明浮窗
  - macOS: NSEvent globalMonitor / Linux: XInput/libinput
- [ ] **G.3** 光标高亮（Cursor Spotlight）
  - 光标周围保持明亮圆圈
  - 其余区域自动变暗
  - 可配置半径和暗度
- [ ] **G.4** 自动缩放（Auto Zoom）—— 远景，复杂度高
  - 追踪光标位置
  - 平滑缩放跟随
  - Metal/Vulkan GPU 渲染
  - 可配置缩放倍率和缓动曲线

---

## 🎨 v0.9.0 — 粗剪导出 + LUT 滤镜 + 云端分享

> 预估工时：**7-10 天**
> 核心卖点：后期处理与导出全流程——修剪、滤镜、分享一站式完成

### Phase H：粗剪导出（Quick Trim & Export）

> 录制结束后快速修剪首尾 → 导出 MP4/GIF

- [ ] **H.1** 创建 `trim-preview.html` + `TrimPreview.svelte`
  - 内嵌 `<video>` 播放器
  - 双滑块 Range Slider（起始帧 / 结束帧）
  - 实时预览修剪区间
- [ ] **H.2** Rust 端：`trim_video(input, start, end, output)` 命令
  - macOS: AVAssetExportSession stream copy（极速无重编码）
  - Linux: ffmpeg -ss -to -c copy（同样零重编码）
- [ ] **H.3** GIF 导出优化
  - 调色板优化（two-pass palettegen + paletteuse）
  - 最高 30fps，自动降帧
  - 文件大小预估
- [ ] **H.4** 录制结束自动弹出 Trim 窗口（可在设置中关闭）

### Phase I：LUT 滤镜

- [ ] **I.1** 解析 `.cube` LUT 文件
- [ ] **I.2** macOS: Core Image `CIColorCubeWithColorSpace` 实时滤镜
- [ ] **I.3** Linux: GStreamer `videobalance` + 自定义色彩映射
- [ ] **I.4** 预设滤镜库：电影感 / 复古 / 高对比 / 暖色 / 冷色
- [ ] **I.5** 导出时应用滤镜（后处理，不影响录制性能）

### Phase J：云端分享

- [ ] **J.1** 一键上传到云端（S3/R2/自定义端点）
- [ ] **J.2** 生成分享链接（类似 Loom/CleanShot Cloud）
- [ ] **J.3** 链接有效期设置（1天/7天/30天/永久）
- [ ] **J.4** 自托管方案（用户自建存储后端）

---

## 🏆 v1.0.0 — 稳定版发布

> 目标：功能完整、性能极致、跨平台稳定的开源截屏录屏工具

### 发布前清单

- [ ] 全平台测试矩阵通过
  - macOS: Apple Silicon (M1-M4) + Intel | macOS 13-15
  - Linux: Ubuntu 22.04/24.04 GNOME | Wayland + X11
- [ ] 性能指标达标
  - macOS: CPU < 3%, 内存 < 50MB, 60fps 零丢帧
  - Linux: CPU < 10% (硬件编码), 60fps
- [ ] 自动更新端到端验证
- [ ] 完整文档（中英双语）
- [ ] 发布到 Homebrew (macOS) + AUR/PPA (Linux)
- [ ] 上架 Product Hunt / Hacker News
- [ ] 项目官网上线（landing page）

---

## 竞品功能对比

| 功能 | CleanShot X | Screen Studio | Loom | **Zureshot** |
|------|------------|---------------|------|-------------|
| 截图 | ✅ | ❌ | ❌ | ✅ |
| 录屏 | ✅ | ✅ | ✅ | ✅ |
| 摄像头气泡 | ❌ | ✅ | ✅ | 📋 v0.6.0 |
| 截图钉桌面 | ✅ | ❌ | ❌ | 📋 v0.6.0 |
| 粗剪导出 | ✅ | ✅ | ✅ | 📋 v0.9.0 |
| 虚拟背景 | ❌ | ✅ | ✅ | 📋 v0.7.0 |
| OCR 文字识别 | ✅ | ❌ | ❌ | 📋 v0.7.0 |
| 实时标注 | ✅ (截图) | ❌ | ❌ | 📋 v0.8.0 (录屏) |
| 自动缩放 | ❌ | ✅ | ❌ | 📋 v0.8.0 |
| 按键显示 | ❌ | ✅ | ❌ | 📋 v0.8.0 |
| LUT 滤镜 | ❌ | ✅ | ❌ | 📋 v0.9.0 |
| 云端分享 | ✅ (付费) | ❌ | ✅ | 📋 v0.9.0 |
| **开源** | ❌ | ❌ | ❌ | **✅** |
| **跨平台** | ❌ macOS only | ❌ macOS only | ✅ (Web) | **✅ macOS + Linux** |
| **免费** | ❌ $29 | ❌ $89/年 | ❌ $15/月 | **✅ 完全免费** |

---

## 优先级排序原则

1. **差异化 > 追赶** — 优先做竞品都没有的功能组合（开源 + 跨平台 + 摄像头 + OCR）
2. **前端优先 > Rust 重写** — 能用 WebView 解决的不写 native 代码（摄像头、标注）
3. **MVP > 完美** — 每个功能先出 Tier 1 最简方案，再迭代到 Tier 2/3
4. **用户反馈驱动** — v0.5.x 收集社区反馈后调整路线图优先级
